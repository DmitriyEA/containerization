Задание 1:
Развернуть и запустить контейнер с любым вашим образом(сайт на CMS, образ ОС, образ базы данных, что угодно). Использовать Docker Compose.
Никаких Dockerfile в этом задании! Говорилось же на семинаре, что Compose!

Задание 2*:
Установить 2 операционную систему в виртуальную машину(создать новый образ ОС). Создать кластер из двух машин(первой и второй), Сделать так, чтобы каждая нода могла управлять кластером (была лидером). Также необходимо добавить каждой ноде по метке: prod, stage или любые другие метки, но у каждой ноды должна быть своя отдельная метка. Проверить и убедиться, что метки действительно добавились. Раскатать одно, хотите два, но хотя бы одно любое приложение на эти две машины (поднять докер сервис) . Использовать Swarm исключительно - читайте название занятия, никаких Kubernetes!


VM ubuntu		VM ubuntu2
192.168.0.44		192.168.0.45


Описание скрншотов:

001 файл конфигурации и запуск проекта
002 все запустилось но нет доступа из-за порта
003 остановка, изменение порта, запуск
004 доступ к админке открыт
005 админка работает
006 доступ к сайту на виртуальной машине
007 инициализация кластера (desktop) и подключение узла (desktop2)
008  результат предыдущих действий
009 изменения статуса ноды
010 добавление меток
011 раскатка
012 раскатка

Ефимов Дмитрий
